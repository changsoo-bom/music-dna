# YouTube Data API v3 키. `pnpm enrich` 가 카탈로그의 youtubeId·duration 을 채울 때만 쓴다.
#
# **서버 전용이다. NEXT_PUBLIC_ 접두사를 절대 붙이지 않는다** — 붙이는 순간
# 클라이언트 번들에 실려서 누구나 꺼내 쓸 수 있다. 앱 실행에는 필요 없고,
# 카탈로그를 늘릴 때만 있으면 된다.
#
# 환경변수로 직접 넣어도 된다(CI 는 그쪽을 쓴다). 이 파일은 값을 담지 않는다.
YOUTUBE_API_KEY=
